# Variables d'environnement
.env
.env.local
.env.*.local

# Note : js/firebase-config.js EST versionné. La configuration web Firebase
# n'est pas un secret ; la sécurité repose sur les règles Firestore
# (firestore.rules) et la restriction de la clé d'API (domaines autorisés).

# Dépendances éventuelles
node_modules/

# Système / éditeurs
.DS_Store
Thumbs.db
*.log
